{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/Lincyaw/workbuddy/schemas/agent.schema.json",
  "title": "Workbuddy Agent (Batch 1 — issue #204)",
  "description": "Schema for the YAML frontmatter of .github/workbuddy/agents/*.md. The Markdown body below the closing `---` is descriptive prose only and is not validated by this schema. Keep this in sync with internal/config/types.go (AgentConfig).",
  "$comment": "Batch 1 schema. Future batches will add `context:` (template field allowlist) and `state:` triggers.",
  "type": "object",
  "additionalProperties": false,
  "required": ["name", "description", "triggers", "role", "runtime", "policy"],
  "properties": {
    "name": {
      "type": "string",
      "minLength": 1,
      "description": "Stable identifier referenced by workflow states. Should match the file basename stem."
    },
    "description": {
      "type": "string",
      "description": "Human-readable summary, displayed in `workbuddy status` and similar surfaces."
    },
    "role": {
      "type": "string",
      "enum": ["dev", "review"],
      "description": "Role catalog is intentionally tiny (see docs/decisions/2026-04-15-agent-role-consolidation.md)."
    },
    "runtime": {
      "type": "string",
      "enum": ["codex", "claude-code", "claude-agent-sdk"],
      "description": "Agent execution backend."
    },
    "runner": {
      "type": "string",
      "enum": ["local", "github-actions"],
      "description": "Where the runtime executes. Defaults to local."
    },
    "triggers": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["label"],
        "properties": {
          "label": {
            "type": "string",
            "minLength": 1,
            "description": "Issue label that hands work to this agent. Must match a workflow state's enter_label."
          },
          "event": {
            "type": "string",
            "enum": ["labeled"],
            "description": "Event currently always `labeled`. Future batch may broaden the set."
          }
        }
      }
    },
    "prompt": {
      "type": "string",
      "description": "Go text/template rendered against runtime.TaskContext at dispatch time."
    },
    "command": {
      "type": "string",
      "description": "Legacy command-template alternative to prompt. Kept for back-compat."
    },
    "policy": {
      "type": "object",
      "additionalProperties": false,
      "required": ["sandbox", "approval", "timeout"],
      "properties": {
        "sandbox": {
          "type": "string",
          "enum": ["read-only", "workspace-write", "danger-full-access"]
        },
        "approval": {
          "type": "string",
          "enum": ["never", "on-failure", "on-request", "via-approver"]
        },
        "model": {
          "type": "string"
        },
        "timeout": {
          "type": "string",
          "pattern": "^[0-9]+(ns|us|µs|ms|s|m|h)$",
          "description": "Go duration string. Should be >= worker.stale_inference.idle_threshold to avoid watchdog kills (validator code WB-S001)."
        }
      }
    },
    "permissions": {
      "type": "object",
      "additionalProperties": true,
      "description": "Subprocess capability boundaries. See internal/config/types.go PermissionsConfig for the full structure."
    },
    "github_actions": {
      "type": "object",
      "additionalProperties": true,
      "description": "Knobs used when runner=github-actions."
    },
    "output_contract": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "schema_file": {
          "type": "string",
          "description": "Path (relative to the agent file) of a JSON schema describing the structured output."
        }
      }
    },
    "timeout": {
      "type": "string",
      "pattern": "^[0-9]+(ns|us|µs|ms|s|m|h)$",
      "description": "Deprecated alias of policy.timeout, kept for back-compat."
    }
  }
}
