import { Layout } from '../components/Layout';

export function Sessions() {
  return (
    <Layout>
      <h1>Sessions</h1>
      <div class="empty">
        Session list ships in a follow-up issue. Until then, link out from the
        dashboard's Session column to inspect a specific run.
      </div>
    </Layout>
  );
}
