# Workbuddy environment configuration
# Each workbuddy instance runs with its own config.

environment: dev          # dev | staging | prod
repo: Lincyaw/workbuddy
poll_interval: 30s
port: 8090                # HTTP audit server port
